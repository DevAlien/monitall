export default async function SiteHomePage({
  params,
}: {
  params: { domain: string };
}) {
  //   if (!data) {
  //     notFound();
  //   }

  return (
    <>
      <div className="mb-20 w-full">site</div>
    </>
  );
}
